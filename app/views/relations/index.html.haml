%h1 Listing relations

%table
  %tr
    %th Object url
    %th Subject url
    %th Predicate
    %th Createor url
    %th
    %th
    %th

  - @relations.each do |relation|
    %tr
      %td= relation.object_url
      %td= relation.subject_url
      %td= relation.predicate
      %td= relation.creator_url
      %td= link_to 'Show', relation
      %td= link_to 'Edit', edit_relation_path(relation)
      %td= link_to 'Destroy', relation, :confirm => 'Are you sure?', :method => :delete

%br

= link_to 'New Relation', new_relation_path
